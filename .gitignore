# yukiakari 项目 .gitignore
# 目的:防止生成文件/敏感密钥被误提交(安全底线:密钥/数据库密码放环境变量,禁止硬编码或提交 Git)

# --- 敏感配置 / 密钥 ---
# config.js 由 Vercel 构建脚本在部署时生成(写入 SUPABASE_URL / SUPABASE_ANON_KEY),
# 本地开发用的真实值也不应提交,见 web/config.js 内注释说明。
web/config.js
.env
.env.*
!.env.example

# --- Supabase 本地开发产物 ---
supabase/.branches
supabase/.temp

# --- 系统/编辑器产物 ---
.DS_Store
Thumbs.db
*.log
.vscode/
.idea/

# --- Node(如未来构建脚本引入依赖) ---
node_modules/

# --- Python(scripts/ 下脚本运行产生的缓存) ---
__pycache__/
*.pyc

# --- 字体子集化源文件(体积太大不适合进 Git,约 24.5MB/份) ---
# scripts/subset-fonts.py 需要这个目录里的两个 Noto Serif CJK JP 源字体才能
# 重新生成 web/assets/fonts/ 下的 woff2 子集,但源字体本身不需要(也不应该)
# 提交进仓库——这正是本轮字体子集化要解决的"体积膨胀"问题,如果把 49MB 的
# 源字体提交进 Git 历史,等于把问题从"网页加载慢"搬到了"仓库体积爆炸",
# 得不偿失。获取方式见 scripts/subset-fonts.py 文件头部注释。
scripts/font-src/
