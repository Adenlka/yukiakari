// 占位文件:查询预约 Edge Function(lookup-reservation/index.ts)
//
// 用途:对应 docs/yukiakari_设计方案_v1.md 第三节 —— 前端不能直接 SELECT
// reservations 表(哪怕加 WHERE 条件,本质还是把整表暴露给客户端过滤)。
// 这个函数接收"预约码 + 手机号",内部做精确匹配,只返回需要展示的字段,
// 匹配不到就返回空,不透露"是code错还是手机号错"(防止被暴力试探)。
//
// 安全要点(实现阶段务必遵守):
// - 参数化查询,不手拼 SQL 字符串
// - 建议加简单的 IP 限流,防止被用来暴力枚举预约码/手机号组合
// - 错误信息不暴露堆栈/SQL 细节
//
// 密钥:如需绕过 RLS 使用 service_role key,只能通过本函数的环境变量读取,
// 绝不能出现在 web/ 前端代码或 Git 仓库里。

// TODO(角色3下一阶段): 实现查询预约逻辑
